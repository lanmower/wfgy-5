## 2026-08-07
- Checked onestardao/WFGY upstream for progress since the last fetch (2026-07-10, 4 weeks): zero non-bot commits repo-wide, zero commits touching `Polaris/` at all, upstream `Polaris/` tree structure unchanged (still 46 files, same paths), and the 4 files this repo keeps verbatim (`Polaris/README.md`, `engine/fifth-dimension-engine/README.md`, `experiments/README.md`, `protocols/goal-compiler/README.md`) are byte-identical to current upstream. Latest release is still `v5.0.0-teaser-01` (2026-05-11). Nothing to incorporate this cycle.
- Extended `.gitignore` to cover new gm-plugkit shared-daemon runtime paths (`.agentplug-kv/`, `.gm/gm.db`, `.gm/config-source-cache-*`) introduced by a gm-plugkit version bump (0.1.843 -> 0.1.1201, per-project watcher -> shared system-wide daemon), which the existing gm-managed gitignore block predates.

## 2026-07-10 (2)
- Removed the example-problems/demo layer from the entire site per explicit user request: Seven Millennium Problems, Baseline-50/Frontier-50, High-Strangeness Math Lab, and Everyday Shoot Modes no longer appear anywhere. Removed `wfgy-core/Polaris/demos/` and `wfgy-core/Polaris/frontier-challenges/` from disk, dropped `demos`/`frontier-challenges` from `theme.mjs`'s `ARCHIVE_DIRS`, and rewrote `home.yaml`, `highlights.yaml`, `skills.yaml`, `research.yaml`, `original.yaml`, `site.yaml`, `paper.md`, and `README.md` to describe the Goal Compiler / Fifth-Dimension Engine / WFGY-Method protocol abstractly, without naming specific example problems.
- Updated `test.js` with a new assertion (`no named example problems remain on any scanned page`) scanning skills/index/research/highlights output for Millennium Problems/Baseline-50/Frontier-50/High-Strangeness/Everyday Shoot terms; full suite passes 9/9. Internal link-integrity check: 11 total links, 0 broken (down from 95 total/0 broken before the demos removal, since demos/frontier-challenges archive pages no longer exist).

## 2026-07-10
- Fixed the actual root cause of the site still reading as Avatar-era: `src/theme.mjs` hardcoded `ARCHIVE_ROOT = 'wfgy-core/Avatar'` and `ARCHIVE_DIRS = ['research','highlights','docs','eval','community']`, completely independent of `config/pages/original.yaml` — every prior yaml edit to the archive section list was silently dead code, and every build wrote empty 0-document stub pages under `archive/research`, `archive/docs`, etc. regardless of yaml content.
- Rewrote `theme.mjs`'s archive manifest builder to target the real `wfgy-core/Polaris` directory tree (`protocols`, `engine`, `demos`, `frontier-challenges`, `experiments`) with a recursive README-walk (`walkReadmes`) supporting arbitrary nesting depth (Polaris nests READMEs, e.g. `demos/seven-millennium-problems/native-lean/README.md`, unlike Avatar's flat per-directory layout), fixed per-doc link resolution and archive-shell depth calculation for nested slugs, and removed the dead `avatar.txt` download block (no Polaris equivalent exists).
- Fetched 15 real README.md/txt documents from `onestardao/WFGY`'s `Polaris/` tree via the GitHub API (`demos/*`, `engine/fifth-dimension-engine`, `frontier-challenges/*`, `protocols/goal-compiler/*`, `experiments/README.md`) to replace two dead 404-stub files and two exact-duplicate flat copies that were previously the entire local Polaris corpus.
- Rewrote `config/globals/site.yaml` (title/description/keywords still said "Avatar"), `config/globals/navigation.yaml` footer, `config/pages/home.yaml` (features/routes/recovery sections were 100% Avatar persona-runtime content under `// why avatar is different`), `config/pages/skills.yaml` (all 12 items linked to the archived `onestardao/WFGY/tree/main/Avatar`), `config/pages/highlights.yaml` (all 8 items described Avatar's WFGY_BRAIN/persona system), `config/pages/research.yaml` (47 fake Avatar SRD-law document entries replaced with the real 15-document Polaris corpus), and `config/pages/original.yaml` (archive sections/downloads pointed at nonexistent Avatar dirs).
- Removed 5 stale, still-discoverable Avatar-era skill files sitting loose at `skills/*.md` (`avatar-boot.md`, `behavior-tuning.md`, `multilingual-calibration.md`, `persona-branching.md`, `persona-recovery.md`) and fixed `skills.yaml` skill hrefs that pointed at a nonexistent local `./skills/<name>/SKILL.md` site path (skills aren't published under `dist/`) to the real GitHub source.
- Verified via full rebuild + content scan: all 6 site pages (home/paper/highlights/research/skills/original) render zero stale Avatar/persona/WFGY_BRAIN artifacts outside legitimate historical Avatar-vs-Polaris comparison text in `paper.md` and the release timeline.

## 2026-07-09 (2)
- Pivot from Avatar to Polaris Protocol: restored flatspace site infrastructure from d2f92e2 commit and reoriented the entire site to present WFGY 5.0's active flagship (Polaris Protocol and Fifth-Dimension Engine) instead of the archived Avatar.
- Fetched core Polaris artifacts from onestardao/WFGY upstream (README.md, Goal-Compiler.md, Fifth-Dimension-Engine.md, plus directory structure for protocols, engine, experiments, demos, frontier-challenges).
- Updated all project metadata: `SKILL.md` (wfgy-5-0-polaris), `README.md` (describes Polaris + site + wfgy-method skill), home page config (heading, badges, CTA), `paper.md` (complete rewrite covering Polaris mechanisms, Fifth-Dimension Engine, drift control, Goal Compiler, and relationship to archived Avatar).
- Verified `skills/wfgy-method/` is unrelated to either Avatar or Polaris — a portable reasoning-discipline skill reusable in any project.

## 2026-07-09
- Removed Avatar site project in initial commit, keeping only `skills/wfgy-method/` — now being reversed to build Polaris Protocol site.

## 2026-07-07 (2)
- Add `skills/wfgy-method/`, a portable Claude Code Agent Skill implementing WFGY's (Wan Fa Gui Yi, `onestardao/WFGY`) core reasoning discipline for any project's own multi-step agent work — conformant to the agentskills.io specification (researched directly against all 9 of its documentation pages), not scoped to this site's own maintenance.
- Ports the genuinely portable behavioral core of BBMC (goal comparison), BBPF (multi-path exploration before ambiguous decisions), and BBCR (checkpoint-and-bounded-retry on detected incoherence), plus the drift trend classifier (convergent/recursive/divergent/chaotic) and a subset of the ProblemMap failure-mode taxonomy, all traced to real, directly-fetched WFGY source files.
- BBAM (attention modulation) and Delta-S both rely on real computation (attention-weight statistics, cosine similarity between embeddings) a text-generating agent cannot perform on itself — rather than silently drop or fake them, each is explicitly reframed in the skill as delegated judgment the calling agent performs directly, captioned as such. `references/honesty-and-provenance.md` documents exactly what was kept, reframed, or rejected — including two manipulation patterns (a hardcoded fake-benchmark demo, a user-skepticism-deflection script) found in the source project's own prompt file and deliberately not reproduced.
- Closed the honesty gap for real: `C:\dev\rs-plugkit` already had a working embedding backend (bundled BGE-small-en-v1.5 BERT model) and vector search, unexposed as a standalone verb. Added `embed` and `similarity` dispatch verbs (verified via real `cargo check`/`cargo build --release` against the wasm32-wasip1 target, then a live end-to-end dispatch after release: a semantically-similar text pair scored `distance: 0.128`, an unrelated pair scored `distance: 0.669`) so the skill's Delta-S is a genuine `1 - cos(I, G)` measurement when a gm-plugkit spool is present, with an honest qualitative fallback when it isn't.
- Adds a durable, project-local self-learning surface (`.wfgy/lessons.md` in whatever project the skill is applied to) as this project's own extension on top of the adapted technique, labeled as such rather than attributed to WFGY's documented mechanism.
- Fixed a real bug caught by a fresh build + link-integrity check: the root archive README page's sibling-directory links used a one-level-up relative path that resolved outside `dist/` instead of to the sibling `archive/<dir>/index.html` pages at the same depth.

## 2026-07-07
- Surface the complete original WFGY archive as real, organized pages instead of hand-summarized YAML paraphrases: `buildArchiveManifest()` walks all 5 wfgy-core/Avatar directories (research, highlights, docs, eval, community) and a new `markdownToHtml()` parser generates one real page per document (67 content docs + 5 hub pages = 72 pages), plus a direct `avatar.txt` download and a root README page.
- New markdown parser handles headings, paragraphs, ordered/unordered lists, blockquotes, horizontal rules, tables, code fences, and inline bold/italic/code/link spans; strips `AI_NOTE` HTML-comment headers; preserves original emoji verbatim as third-party archived source content.
- `resolveArchiveLink()` rewrites every source-relative `./x.md`/`../y/z.md` cross-reference to its generated page URL — crawled all 72 pages and checked 3077 internal links, zero broken.
- Fixed a real bug: `highlights.yaml`'s `name` field is a human Title-Case label that doesn't match the real kebab-case file slug (unlike `research.yaml`, where they happen to be identical) — added an explicit `slug` field per item so all 8 highlights links resolve.
- Rewrote `original.html` from a flat 8-item hardcoded directory list into a real file browser: 5 sections with live document counts plus direct downloads for `avatar.txt` and the root README.

## 2026-07-06
- Research onestardao/WFGY upstream (releases API, Avatar/README.md, main README) to bring site content current: confirmed Polaris Protocol is the real active flagship (WFGY 5.0 Polaris Protocol), with two named shipped components (Polaris Goal Compiler, Fifth-Dimension Engine) previously absent from the site entirely.
- Corrected an unverifiable "Avatar archived May 5" claim (no such dated announcement exists upstream; May 5 is the CFV Easter Egg release date) with a real 6-entry release timeline (v1.0 through v5.0.0-teaser-01).
- Added WFGY acronym meaning (Wan Fa Gui Yi) and a core-mechanisms explainer (Delta-S drift measurement, BBMC/BBPF/BBCR/BBAM) to paper.md, plus a caveat that published benchmark figures are self-reported/unverified.
- Fixed real bugs surfaced by auditing the anentrypoint-design SDK: Row/RowLink silently drop any `data-cat`/`data-file-type` prop (fixed rest-free destructure) so these attributes never reached the DOM — category coloring now uses Row's real `rail` prop; file-type on original.html uses `dir` (not `directory`) matching the SDK's actual contract, applied via a wrapping element. Hero's unsupported `accentHref` prop replaced with `HeroFromPageData`'s `ctas`. Removed a dead `--prose-stack-md` token override that never existed in the SDK.
- Added a working `ThemeToggle` and interactive `FilterPills` (research/skills pages) — fixed a state-loss bug where the page renderer was re-invoked fresh inside `mount`'s render loop, resetting the category-filter closure on every click.
- Verified live in browser across all 6 pages: zero console errors, correct row/rail/pill counts, working click-to-filter and theme-cycle interactions.

## 2026-05-03
- Fix broken skill hrefs: all 12 skill items now link to upstream GitHub repo (onestardao/WFGY/tree/main/Avatar)
- Add test.js: 8 assertions covering build output invariants (nav, hrefs, research grouping, highlights content)

